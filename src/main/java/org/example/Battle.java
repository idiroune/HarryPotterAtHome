/*package org.example;
import java.util.Random;

public class Battle {

    public void start() {
        Wizard wizard = MakeCharacter.getWizard();

        System.out.println("A " +  + " appears!");

        while (Health > 0 && monster.getHealth() > 0) {

            System.out.println(" You attacks for " + wizard.getWizardDamage() + " damage.");
            monster.takeDamage(playerDamage);

            if (monster.getHealth() <= 0) {
                System.out.println("The " + monster.getName() + " is defeated!");
                break;
            }

            System.out.println("The " + monster.getName() + " attacks for " + monsterDamage + " damage.");
            Health -= 10;

            if (Health <= 0) {
                System.out.println("You is defeated...");
                break;
            }
        }
    }
}*/
